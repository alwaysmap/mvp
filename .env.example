# AlwaysMap Environment Variables
#
# Copy this file to .env and fill in your actual values:
#   cp .env.example .env
#
# NEVER commit .env to git! It should be in .gitignore.

# Printful API Key (get from https://www.printful.com/dashboard/api)
# Use test key for development, live key for production
PRINTFUL_API_KEY=sk_test_your_key_here

# Google Cloud Storage Bucket (for storing exported PNGs)
# Development: use a test bucket
# Production: use a production bucket
GCS_BUCKET=alwaysmap-dev-exports

# Database Password (not needed for local docker-compose, which uses postgres/postgres)
# Only needed for connecting to external database
# DATABASE_PASSWORD=your_secure_password_here

# Google Cloud credentials (optional for local dev)
# Path to service account JSON file
# GOOGLE_APPLICATION_CREDENTIALS=/path/to/service-account.json
