# Database configuration (Postgres-only, no Redis needed)
DATABASE_HOST=localhost
DATABASE_PORT=5432
DATABASE_NAME=alwaysmap
DATABASE_USER=postgres
DATABASE_PASSWORD=postgres
DATABASE_POOL_SIZE=10

# Or use connection string:
# DATABASE_URL=postgresql://postgres:postgres@localhost:5432/alwaysmap

# Export worker configuration
RENDER_BASE_URL=http://localhost:5173
EXPORT_DIR=./exports

# GCP configuration (optional)
GCS_BUCKET=family-maps
GOOGLE_APPLICATION_CREDENTIALS=./gcp-credentials.json

# Printful API (optional)
PRINTFUL_API_KEY=
